/** Space between the focused input and the top edge of the keyboard. */
export const SHEET_KEYBOARD_BOTTOM_OFFSET = 16;

/** Extra scroll padding at the bottom while the keyboard is open. */
export const SHEET_KEYBOARD_EXTRA_SPACE = 24;
